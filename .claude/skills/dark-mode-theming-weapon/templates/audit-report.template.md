# Template: Dark Mode Theming Audit Report

*Fill in during a `dark-mode-theming-guardian` audit. Replace all `{...}` placeholders.*

---

# Dark Mode Theming Audit — {app-name}

**Date:** {YYYY-MM-DD}
**Branch/commit:** {branch-or-sha}
**Auditor:** dark-mode-theming-guardian
**Stack:** {Next.js version} | {next-themes version} | {Tailwind version}
**Router:** {App Router | Pages Router}

---

## Summary scorecard

| Area | Status | Severity of worst finding |
|------|--------|--------------------------|
| Token architecture | {✓ Pass / ⚠ Partial / ✗ Fail} | {High/Medium/Low/None} |
| next-themes wiring | {✓ / ⚠ / ✗} | {High/Medium/Low/None} |
| FOWT prevention | {✓ / ⚠ / ✗} | {High/Medium/Low/None} |
| SSR hydration safety | {✓ / ⚠ / ✗} | {High/Medium/Low/None} |
| Tailwind v4 dark mode | {✓ / ⚠ / ✗} | {High/Medium/Low/None} |
| Multi-brand theming | {✓ / ⚠ / N/A} | {High/Medium/Low/None} |

---

## Findings

### HIGH — {finding title}

**Location:** `{file:line}`
**Issue:** {description of the problem}
**Fix:**
```{language}
{before}
```
→
```{language}
{after}
```

---

### MEDIUM — {finding title}

**Location:** `{file:line}`
**Issue:** {description}
**Fix:** {brief fix description or code block}

---

### LOW — {finding title}

**Location:** `{file:line}`
**Issue:** {description}
**Fix:** {brief fix}

---

## Token diff (before / after)

| Token | Before | After | Reason |
|-------|--------|-------|--------|
| `--color-primary` | `#3b82f6` | `var(--primitive-blue-500)` | Move to primitive reference |
| {token} | {before} | {after} | {reason} |

---

## FOWT checklist

- [ ] `ThemeProvider` in `layout.tsx` / `_app.tsx`
- [ ] `suppressHydrationWarning` on `<html>` / `<Html>`
- [ ] `disableTransitionOnChange` enabled
- [ ] `meta[name="color-scheme"]` in `<head>`
- [ ] No raw hex in component files
- [ ] Visual FOWT test passed (CPU throttled hard refresh)

---

## Recommended next steps

1. {action 1}
2. {action 2}
3. {action 3 — optional}

---

*Generated by `dark-mode-theming-guardian` using `dark-mode-theming-weapon`. See `ai-tools/skills/dark-mode-theming-weapon/` for the full weapon.*
