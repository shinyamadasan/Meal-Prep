# Reports

This folder collects past overlay audit reports produced by `modal-toast-dialog-guardian`.

Each report is a filled-in copy of `templates/overlay-audit-report.md` saved as:

```
reports/YYYY-MM-DD-<branch-or-pr-slug>-overlay-audit.md
```

The folder is empty until the first audit run completes.
