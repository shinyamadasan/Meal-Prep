# Image Optimization Audit Report

**Project:** {project-name}
**Audited by:** image-optimization-guardian
**Date:** {YYYY-MM-DD}
**Branch / commit:** {branch-or-commit}
**Framework:** {Next.js vXX | Astro | SvelteKit | Plain HTML | other}
**CDN:** {Vercel | Cloudflare | Imgix | None}

---

## Scorecard

| Category | Score | Status |
|---|---|---|
| Format selection (AVIF/WebP) | {0-100} | {Pass / Fail / Partial} |
| srcset + sizes correctness | {0-100} | {Pass / Fail / Partial} |
| LCP image priority | {0-100} | {Pass / Fail / Partial} |
| width + height attributes | {0-100} | {Pass / Fail / Partial} |
| Placeholder implementation | {0-100} | {Pass / Fail / Partial} |
| **Overall** | **{0-100}** | **{Pass / Fail / Partial}** |

---

## 1. Image inventory

| File / component | Current format | Size (KB) | Has srcset | Has sizes | Has placeholder | Notes |
|---|---|---|---|---|---|---|
| {path/to/image.jpg} | JPEG | {XX} | No | No | No | {e.g. hero candidate} |
| {path/to/component.tsx} | `<Image>` | - | Auto | {Yes / No} | {Yes / No} | {missing sizes} |

---

## 2. Format breakdown

| Format | Count | % of total | Action needed |
|---|---|---|---|
| AVIF | {N} | {XX}% | {e.g. None — in use} |
| WebP | {N} | {XX}% | {e.g. None — used as fallback} |
| JPEG | {N} | {XX}% | {e.g. Convert {N} to AVIF+WebP} |
| PNG | {N} | {XX}% | {e.g. Check for transparency; convert if none} |
| SVG | {N} | {XX}% | {e.g. None — appropriate format} |
| GIF | {N} | {XX}% | {e.g. Convert animated GIFs to video or WebP animation} |

---

## 3. srcset / sizes audit

**Images missing srcset:**
- {path/to/image.jpg} — currently fixed `src`; no responsive variants exist

**Images with incorrect `sizes`:**
- {`<Image src="..." sizes="100vw" />`} at {path/to/component.tsx}:{line}
  - Actual rendered width: 33vw (3-column grid above 1024px)
  - Correct sizes: `(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw`

**Correctly sized images:**
- {list any that pass}

---

## 4. LCP candidates

| Page | LCP element | Has priority / fetchpriority | Has `loading="lazy"` (anti-pattern)? |
|---|---|---|---|
| `/` | `<Image src="/hero.jpg" ...>` | {Yes / No} | {Yes — REMOVE / No} |
| `/products` | `<Image src="/banner.jpg" ...>` | {Yes / No} | {Yes — REMOVE / No} |

---

## 5. Placeholder audit

| Image | Placeholder type | Status | Notes |
|---|---|---|---|
| {path/component} | None | Missing | Add LQIP via plaiceholder |
| {path/component} | `blurDataURL` (LQIP) | OK | - |
| {path/component} | `blurDataURL` (BlurHash gradient) | OK | - |

---

## 6. width + height audit

**Images missing `width` and `height` (CLS risk):**
- {path/to/image.jpg} — no width/height; estimated CLS contribution: {high/medium/low}

---

## 7. Remediation checklist (prioritized)

> Listed from highest to lowest estimated LCP/CLS impact.

- [ ] **CRITICAL:** Remove `loading="lazy"` from LCP image on `/` (`/app/page.tsx` line {N})
- [ ] **HIGH:** Add `priority` (or `preload` for Next.js 16+) to `/hero.jpg` on homepage
- [ ] **HIGH:** Add `sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw"` to product grid `<Image>` ({N} instances)
- [ ] **MEDIUM:** Convert {N} JPEG images to AVIF+WebP (see `guides/05-tooling.md` for Sharp batch script)
- [ ] **MEDIUM:** Add `width` and `height` to {N} `<img>` elements missing dimensions
- [ ] **LOW:** Add LQIP placeholder to {N} product card images
- [ ] **LOW:** Enable `formats: ['image/avif', 'image/webp']` in `next.config` (currently using defaults)

---

## 8. Estimated impact

| Metric | Before | After (estimated) |
|---|---|---|
| LCP (homepage) | {Xs} | {Xs} |
| CLS (homepage) | {0.XX} | {0.XX} |
| Hero image size (JPEG) | {XX KB} | {XX KB AVIF} (-{XX}%) |
| Product grid image size avg | {XX KB} | {XX KB AVIF} (-{XX}%) |

---

*Generated by `image-optimization-guardian` using `image-optimization-weapon`. See `guides/` for implementation details.*
