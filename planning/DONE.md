# Done — completed work log

> Append-only. The agent adds an entry here at **Task Completion** (newest at top). Full diffs live
> in `git log`; this is the human-readable "what shipped and when". Prune freely — git is the archive.

- ✅ TASK-057 / D-071 — Collection-aware deletion tombstones landed: `d-071-tombstone-namespace` @ `f73ce3c` merged `--no-ff` into `main` at `bd89d5d` and pushed, after an explicit owner authorization recorded on `main` in its own commit `6e28903` ahead of the merge. **The bug was deterministic user-data loss, not a theoretical one**: `AppState.deletions` was ONE flat `{ [rawId]: deletedAtISO }` map consulted against every key in `TOMBSTONE_KEYS`, so deleting recipe `5` also destroyed hack `5`, pantry item `5`, custom ingredient `5`, cooked meal `5` and user ingredient `5` — on this device and every synced one — and seeded recipe ids `1-40` overlap default-hack ids `1-14` completely. Reproduced against real base code at `98cf393` two ways before reviewing the fix: the shipped `applyTombstones()` with a flat `{'5': ts}` wiped all six collections, and the real user path (`deleteSelectedPantryItems()` on pantry item 5) destroyed recipe 5, hack 5 and ingredient 5. A third finding fell out of the same reproduction: the flat baseline also MASKED genuine deletes, because an id still present in another collection kept the vanish-diff from seeing it at all. The fix namespaces the map per collection with centralized helpers (`normalizeDeletions`, `ensureDeletions`, `deletionBucket`, `writeTombstone`, `readTombstone`, `clearTombstone`, `tombstoneCount`) so a raw-id write cannot easily reappear — a repo-wide grep for indexed writes into `AppState.deletions` returns nothing. Migration is knowingly lossy and says so: collection-exclusive prefixes migrate (`flv-`, `cm_`, `ui_`, `buy_`/`ib_`/`staple_`, each re-proven exclusive by inspecting every id-minting site rather than pattern-matching the name), ambiguous numerics/timestamps/imported ids are DROPPED, counted and warned once, with no `_legacy` bucket and no global fallback — **the accepted tradeoff being that some historical ambiguous deletes may resurrect from stale remote copies, which beats continuing deterministic cross-collection destruction**. **Review did not pass on the first candidate, and that is the part worth keeping.** `1f443ac` namespaced the tombstone map correctly and, in the same motion, silently split `MASS_DELETE_GUARD` per collection — reopening the exact phantom-mass-delete class the guard exists to stop. Measured against `98cf393` on an identical fixture (40 recipes, 30 pantry, 14 hacks, 8 ingredients, 3 flavors, 2 cooked meals, 1 user ingredient, all transiently emptied): base wrote nothing, the candidate wrote phantom tombstones for all three flavors, both cooked meals and the user ingredient — real deletes propagated to every device, hitting the just-shipped Flavor Library hardest because small collections are the ones that fall under a per-collection threshold. The suite was 401/401 green through all of it; no test covered a multi-collection transient race. A second finding, `loadFromLocalStorage()` gaining `purgeOldTombstones()`/`applyTombstones()` outside D-071 scope, changed signed-out behaviour (a re-added `staple_salt` survived on base and vanished on the candidate) and was load-bearing for exactly one test. `f73ce3c` repaired both: the guard sums vanished ids across all collections into `totalVanished` and compares that single aggregate before writing anything, returning with `_idBaseline` wholly unchanged on a trip exactly as the flat implementation did (constant still `5`), and the signed-out side effect is gone — `loadFromLocalStorage()` is now byte-identical to base but for the one required `normalizeDeletions()` call. It also fixed, unprompted, the reviewer's P2 that the delivered BASE REPRO and MUTATION tests re-implemented the old filter inline rather than mutating production code; both now mutate the real function source and restore it in `finally`, and a new mutation test bypasses the aggregate guard and asserts the phantoms reappear. Both invariants were negative-proofed by mutating PRODUCTION code, not simulations: collapsing `applyTombstones()` to a flat map turns 11 tests red with the exact collateral signature; reverting the guard to per-collection turns 3 red with the phantom signature. Running the re-review spec unmodified against the real pre-repair commit failed exactly the 4 tests targeting the two defects and passed every isolation/migration/LWW/purge/explicit-writer test, confirming the spec targeted the repair and nothing else moved. Verified end to end: `npm test` and `npm run test:local` 404/404 on merged `main`, focused deletion/sync specs 154/154, suite-classification green, `Verify-Decisions.ps1` 41/41 (three new pointers added by this record all hold), `git diff --check` clean. **The first push-triggered CI run failed and is recorded as-is, not re-run for green**: run `33000618114` attempt 1, local gate 401 passed with 3 `waitForRestored()` timeouts (`bulk-add-partial-retry:416`, `flavor-library:328`, `inventory-quantity-truth:81`), production gate skipped because the local gate runs first. Diagnosed rather than waved away: two of the three specs are byte-untouched by D-071 and the third's failing test is untouched (only tombstone tests changed in that file), none of the three predicates involve `AppState.deletions`, `normalizeDeletions()` measures 0.0004-0.004 ms and cannot contribute to a 30,000 ms timeout, and `bulk-add-partial-retry.spec.js:416` — same test, same line — already failed on `main` at run `32899800754` before D-071 existed. Pre-existing D-065 reload-race class, amplified by the suite growing 381→404 tests; not a product regression. GitHub Pages deployed successfully in run `33000615788`; all five served assets (`app.js`, `index.html`, `style.css`, `sw.js`, `manifest.json`) match landed `main` after line-ending normalization, and the deployed bundle contains every D-071 helper plus the aggregate guard with zero occurrences of the old per-vanish guard or raw-id writes. Production smoke against the deployed build: 137 passed / 4 skipped / 0 failed, plus a targeted serial re-run of the two specs that stalled under parallel load (26/26, including `kitchen-truth:259` bulk cleanup crossing the guard live). Ten further live proofs run against the deployed URL — cross-collection isolation both directions, flavor isolation, prefix normalization, ambiguous-numeric drop, aggregate transient-empty guard, below-guard delete, LWW, no console errors. Carried forward, deliberately NOT fixed: **more than `MASS_DELETE_GUARD` genuine vanish-diff deletes can still be suppressed indefinitely** (predates D-071, now in ROADMAP Known Issues and D-071 § Residual limitation); `restoreBackup()` still does not restore deletions and `exportData()` still omits them, both investigated and left because fixing either expands product behaviour beyond deletion identity; old clients preserve but do not honor nested tombstones until they update; and the eight live D-071 proofs are not yet pinned as a committed production-smoke spec. `wave1-portion-truth` remains parked at `88b5598`. (2026-08-26)
- ✅ D-070 — Flavor Library wave landed: `wave-flavor-library` @ `54099ce` reviewed clean and merged `--no-ff` into `main` at `b219e20`, then pushed to `origin/main`. The feature shipped as a top-level synced `AppState.flavors` collection with `flv-` ids, complete localStorage/import/export/backup/Firestore/realtime persistence, opt-in starter flavors, and no auto-seeding. Pre-merge local verification was green: Flavor Library 47/47, local suite 382/382, full `npm test` 382/382, suite-classification 6/6, and decision verification passed. First push-triggered CI was recorded as-is and failed in run `32983219373`: local suite 381 passed, one timeout in `tests/inventory-quantity-truth.spec.js` at `waitForRestored()`, so Pages-wait/prod-smoke steps were skipped and no `workflow_dispatch` run was started. GitHub Pages deployed the pushed SHA successfully in run `32983147959`; served `index.html`, `app.js`, and `style.css` match landed `main` after line-ending normalization. Carried forward deliberately: D-071 open, Ready Food → "Try with" / Meal Lego / free-text protein inference deferred, and `wave1-portion-truth` untouched at `88b5598`. (2026-08-26)
- ✅ TASK-056 — Reload-state harness audit (D-065 addendum 2): TASK-055 named the remaining reload sites as residual risk and left them, on the defensible grounds that none had been observed failing — and that held for exactly one CI run. The next push-triggered gate went red on `bulk-add-partial-retry.spec.js:426` with `Expected ["Chicken","Eggs","Milk"] / Received []`, the identical signature in one of the named sites, which is the lesson worth keeping: once a failure class is PROVEN, finishing its tail is not optional cleanup. **The previous census was also wrong, in both directions** — recounted from source there are **16 `page.reload()` sites across 13 local specs**, not eleven across ten: three were already fixed, and two more (`ready-food-portions`, `recipe-edit-preservation`) already carried correct inline restore-waits and had simply not been recognised as such. An audit that trusts its own earlier inventory is not an audit. Every site was classified BEFORE anything was edited: **14 Class A** (assertion depends on restored state — 3 done previously, **11 migrated here**), **1 Class B** (boot-only), **1 Class C** (harness defect, same site as B), **1 Class D** (absence claim). Result: **14 of 16 now use `waitForRestored()` and ZERO reload sites read persisted state after a bare `waitForAppReady()`**. Two judgements carried the work. First, **identity not fields**: the default predicate waits for the record to EXIST and leaves field checks to `expect()`, because a row restores atomically out of one `JSON.parse` — so identity proves restoration while keeping a genuine persistence bug a readable diff instead of an opaque timeout. Second, **two deliberate exceptions** where identity cannot distinguish restored from re-seeded: `seed-isolation`'s edited recipes (a fresh seed also has ids 27 and 5, so the predicate must name `favorite === true` and `currentServings === 6`) and `starter-pack`'s saved id set (a fresh seed also yields 40 ids); getting those wrong would have produced two tests that pass whether or not restoration happened, which is precisely the false-positive class this work exists to remove. Deliberately NOT changed: **Class B** `cook-method-discovery`, whose reload asserts `recipeQuickFilter === ''` — a module-scoped variable a reload clears unconditionally, so no restored state is involved and `settled()` is the honest wait; it was not converted for consistency's sake. **Class C** on that same spec WAS fixed: it was the only one of thirteen with no bootstrap sentinel, so `localStorage.clear()` ran on every navigation including reload — not overclaimed as a false positive today, because its assertion holds either way, but it primed the file for one, and the sentinel now matches all twelve siblings with no assertion touched. **Class D** `seed-isolation:239` was left alone: it asserts a deliberately empty recipe list is not re-seeded, there is no predicate for "the thing that must not happen has finished not happening", and inventing a completion signal would be a product change under a freeze — recorded as the one remaining special-case risk on the reload surface. Reproduced **at the exact failing site** this time, which is stronger than TASK-055 managed: A/B against the same 20×-throttled page, ten runs — bare `waitForAppReady()` failed **4/10** and what it read was `[]`, the CI symptom verbatim, while `waitForRestored()` failed **0/10**; the abort-Firebase control was 0/10 for both, so the honest position is unchanged — class reproduced, runner condition still inferred. Negative-proofed in one sweep across every predicate shape (pantry-by-name, pantry-by-id, recipe-by-id, cookedMeal-by-id, alert ledger, edit-witness, id-set): sabotaging all restored collections makes all eleven migrated waits time out naming their missing state, so no new wait is decorative. **No assertion added, removed or altered anywhere** — confirmed by grepping `expect(` over the whole diff; two `waitForTimeout(300)` removed, both trailing settles inside the inline restore-waits folded onto the shared helper, none added, and no unrelated interaction or animation wait touched. `waitForAppReady()` is unchanged — `tests/app-ready.js` has an empty diff on this branch — and **retries remain disabled** (`playwright.config.js` has no `retries` key). Reversible D-032 `done`; product source byte-unchanged (`git diff 8b13ddf -- app.js index.html style.css sw.js manifest.json` empty). Focused ×10 on all ten touched specs, the previously failing test ×20 green, 13 reload specs together at `--workers=2` ×2 (254 passed each), `npm run test:local` and `npm test` 335/335, suite-classification 6/6, `Verify-Decisions.ps1` 30/30. Merged `--no-ff` at `92dbdea` with `ac64da8` unchanged. **The first push-triggered CI run was green on attempt 1 with no retries** — local gate 335 passed at 2 workers, production gate 137 passed, `bulk-add-partial-retry` passed, the three TASK-055 specs passed, and the production gate followed the local gate normally for the first time in three landings. One green run is not proof an intermittent class is gone; the next several ordinary push-triggered runs are the actual evidence, and no commits will be manufactured to produce them. Final rule recorded: *after `page.reload()`, a test that depends on persisted application state must wait on that specific restored state — boot/render completion is not evidence of persistence restoration.* Carried forward: `seed-isolation:239` (the one remaining special-case risk), the still-inferred runner condition behind the >150 ms gap, the short 200–600 ms interaction waits (none implicated), `loadFromLocalStorage()`'s silent exception swallowing (product code), and `wave1-portion-truth` parked at `88b5598`. (2026-08-25)
- ✅ TASK-055 — Test gate determinism (D-065 addendum): the deterministic local branch gate went red twice on `56d8da7` with the product source **byte-identical** to a run that had passed — `kitchen-truth` (`pantryHas: false`), `low-effort-metadata` (`TypeError: reading 'equipment' of undefined`), then `cook-depletion-tombstones` (`pantryIds: []`). Different specs each attempt, all three asserting immediately after `page.reload()` + `waitForAppReady()`, all three green locally on repeat, and a `workflow_dispatch` on the identical commit green. A gate that behaves like that is worse than no gate, because it teaches you to re-run instead of to read. Characterised from the real CI logs before touching anything, and the finding is that these are **one cause, not three**: `initApp()` ends with `showTab('dashboard')` → `renderDashboard()` UNCONDITIONALLY, on whatever `AppState` holds at that moment, so when `window.firebase` is present and the restore happens later inside the async `onAuthStateChanged` callback, the dashboard paints — and `waitForAppReady()`'s condition goes true — against a still-default `AppState`. Measured on this app: readiness at 346ms, the saved pantry landing at 406ms. The only thing covering that 60ms gap was the helper's trailing `waitForTimeout(150)` — **elapsed time standing in for a state check**, the exact substitution D-065 replaced 2500ms waits to avoid; it survived because 150ms is generous on a developer machine and is not on a loaded two-worker runner. `cook-depletion-tombstones` asserts `storedPantryIds` straight out of localStorage BEFORE it reloads and that assertion passes, which independently rules out "the save never happened". Reproduced rather than assumed: under 20× CPU throttling with 8-way parallelism the pantry read back empty after reload — the exact CI symptom — and an A/B harness running both strategies against the SAME throttled page ten times gave old pattern **2/10 failed**, waiting on the state itself **0/10**. Honest limit stated rather than buried: that reproduction needed the async-init path, and the three specs abort Firebase where `initApp()` is fully synchronous and readiness genuinely implies restore, so **the failure class is reproduced while the exact condition that opened the gap past 150ms on the runner remains inferred**; ruled out along the way were a `saveData()` debounce (`saveToLocalStorage()` is synchronous), service workers (do not register under `file://`, and `sw.js` passes cross-origin through), static `#dashboard` markup (the element is empty in `index.html`), and a reseeding init script (all three bootstrap guards are correct). The fix does not depend on the answer: if the state truly never arrives, the wait now times out NAMING that state instead of producing an assertion diff against an empty `AppState`. `tests/app-ready.js` gains `waitForRestored(page, predicate)` — `waitForAppReady()` plus the caller's own narrowest condition — applied at the three proven sites (Chicken Breast pantry record + checked grocery row; recipe `ip-1`; pantry non-empty + all six tombstones). `waitForAppReady()` itself is **unchanged**: "booted and painted" is still the right contract for the ~300 tests that need only that, and narrowing it globally to "a saved document is loaded" would have been the obvious move and would have broken the specs that deliberately boot an empty or seeded document. No assertion changed or weakened — the spec diff is three `require` lines and three wait calls. Negative-proofed three ways: sabotaging the restored state makes each new wait time out naming it, which matters because the previous harness printed green without exercising the condition, and that is exactly the failure mode being closed. **Retries deliberately still disabled** — none added, none recommended; `playwright.config.js` has no `retries` key so Playwright's default 0 applies, and a retry would have printed green while the tests kept asserting against pre-restore state. Rule established: *a rendered application is not proof that persisted state needed by a test has been restored; tests that depend on restored state must wait on that specific state* — recorded in the D-065 addendum and at the point of use so the next person tempted to collapse `waitForRestored` back into `waitForAppReady` reads why not. Reversible D-032 `done`; product source byte-unchanged (`git diff 56d8da7 -- app.js index.html style.css sw.js manifest.json` empty). kitchen-truth 270/270, low-effort-metadata 50/50, cook-depletion-tombstones 90/90, the three together at CI's 2-worker config 123/123, `npm run test:local` and `npm test` 335/335, suite-classification 6/6, `Verify-Decisions.ps1` 30/30. Merged `--no-ff` at `1cee2d9` with `4f1b9d9` unchanged. **The first push-triggered CI run failed, and it is corroboration rather than falsification**: run `32899800754` attempt 1, no retries, went red on `bulk-add-partial-retry.spec.js:426` with `AppState.pantry` reading back `[]` after reload — the identical signature, in one of the ELEVEN reload sites this wave deliberately did not migrate; all three fixed specs passed, and the production gate was skipped because the local gate runs first and failed, which is D-065's intended ordering rather than a second defect. `workflow_dispatch` on the same SHA (`32900172249`) was green: local 335, production 137, no retries. Recorded rather than re-run for green. Carried forward, deliberately NOT fixed: **eleven `page.reload()` sites across ten specs still use bare `waitForAppReady()`** and carry the same latent exposure (`bulk-add-date-truth` ×2, `inventory-quantity-truth` ×2, `seed-isolation` ×2, `bulk-add-partial-retry`, `cook-method-discovery`, `food-attention-notifications`, `inventory-expiry-display`, `ready-food-portions`, `recipe-edit-preservation`, `starter-pack`) — `bulk-add-partial-retry` has now been observed failing, which promotes that migration from theory to evidence and makes it the next task; also untouched are the short 200–400ms interaction waits (none implicated, and D-065 kept them on purpose), `loadFromLocalStorage()`'s silent exception swallowing (product code), production test classification, and `wave1-portion-truth`. (2026-08-25)
- ✅ TASK-054 — Bulk Add short years (D-067 extension): the same Kitchen Truth failure one door further along. `Eggs 12 pcs Aug 8 26` — how a carton is printed and how a person actually types it — reproduced D-067's original symptom exactly: the whole line became `name`, `inferCategory()` matched "eggs" inside it, `categoryShelfLife('Protein')` supplied 3 days, and the card read "Best by Aug 28 · 3d left" for eggs dated Aug 8. The comma spelling failed more quietly still — `Eggs, 12, pcs, Aug 8 26` parsed name/qty/unit correctly and dropped the fourth field with no warning at all. Characterised on unmodified `main @ 6692e2a` first; root cause was one line twice, both month-word branches of `parseTrailingDate()` hard-requiring `(\d{4})`. **It took two narrowings to be right, both caught in review, both real defects rather than preference calls.** The first version accepted the full 00-99 range and turned `Juice May 5 12` into an expiry in 2012 — juice that went off fourteen years ago, which nobody types into a pantry; `May 5 12` describes a product name far better than a date. So expansion stayed deterministic (26 → 2026, never 1926) and what became bounded is whether the result is *believed*: `shortYearPlausible()` accepts a short year only inside `[currentYear - 1, currentYear + 10]`, relative to the clock rather than a hard-coded range — in 2030 the identical `Aug 8 26` stops parsing and `Aug 8 40` starts. Review then caught the second problem: the window had quietly put a new rung ABOVE the top of the D-067 precedence ladder, holding back `Juice May 5 12 exp:2026-08-08` even though the user had already supplied an unambiguous expiry — and an escape hatch that closes under the one condition you need it for is not an escape hatch. The rejection now fires only when the line carries no valid `exp:`. The half that matters more than the bound: a rejected short year is NEVER swallowed back into the item name, which would be the original defect wearing a new hat. `parseTrailingDate()` gained a third verdict — `{ shortYear: 'nn' }` alongside `{ iso, rest }` and `null` — and the line becomes D-068 `attention`: not persisted, exact original text kept, modal open, note naming both problem and fix ("year \"12\" is outside the expected food-expiry range. Use a four-digit year if you mean 2012."). A rescued line deliberately does NOT strip `May 5 12` from the name: it was never accepted as a date, so removing it would delete text on the strength of a reading the parser just refused. Only `exp:` has that authority — the shared field and shelf-life inference never rescue, because they would stamp a date unrelated to the one the user typed; an invalid `exp:` rescues nothing either, failing on its own terms first. A PLAUSIBLE date plus `exp:` is unchanged from D-067: date stripped, `exp:` wins. What protects product names was never the year's width but the COMPLETE grammar — month WORD and day and year, in trailing position — so `7 Up`, `Heinz 57 Sauce`, `Vitamin B12`, `12 Grain Bread`, `Omega 3 6 9`, `Vitamin 2000`, `Sauce 12 2026` and the new adversarial cases `Formula 26`, `Protein 8 26`, `Sauce Aug 26`, `Vitamin May 26` and `Eggs 8 26` all still come through as plain names; a two-digit number is never a year on its own. Calendar validity is judged BEFORE the year, so `Feb 31 26` stays "not a date at all" and nothing rolls into March, and the `exp:2026-02-31` guard is untouched. Four-digit years are never windowed — `May 5 2012` still stores 2012, `1999` and `2099` likewise — and no general expiry-age restriction was added anywhere. Because the expansion is clock-relative the tests control the clock: `page.clock.setFixedTime` pins 2026 where the literal cases live and 2030 to prove the window moves from ONE input, and one case derives the bounds from the app's own clock so it states the rule rather than a date that rots; the production smokes build their short-year inputs from the DEPLOYED app's clock for the same reason. Mutation-checked nine ways, every one caught: four-digit-only (12 fail), window removed (3), each bound moved one step in each direction (3 each), rejected year buried in the name (7), `exp:` rescue removed (4), rescue extended to the shared field (1), rescued line's name stripped (4) — with every pre-existing case passing under all nine. Three of my own draft assertions were wrong about D-069 and the app was right (twice expecting a merge where a printed expiry correctly forces separation, once expecting a second record where a quantity-less duplicate is correctly skipped); each was corrected to the real rule rather than the code bent to the test, and the D-069 machinery pushing back three times is itself a good sign about it. Recorded as a D-067 ADDENDUM, not a new decision and not a new expiry model. Reversible D-032 `done`, operator-directed landing after a four-round review. Local suite 306→335; production suite 137 passed / 4 skipped; `Verify-Decisions.ps1` all 29 pointers hold. Merged `--no-ff` at `7ce77cc` with `b594e74`, `c649e81`, `3aceda5` and `f2aaca8` unchanged; production smoke follow-up `88d6357` written only after Pages served `7ce77cc`, so the gate was never intentionally red. Final main `88d6357`; served `app.js`/`index.html`/`style.css` verified SHA-256-identical, with `expandYear`, `shortYearPlausible`, `trailingDateVerdict`, the `{ shortYear }` verdict and the updated help copy all confirmed in the deployed bundle. No red-zone line touched — `saveData()` semantics, localStorage, Firestore sync, `cloudReady`, tombstones, `MASS_DELETE_GUARD`, conflict resolution, freshness calculation, pantry rendering, the inventory merge architecture, the D-068 retry machinery, auth and the service worker are all unchanged; `style.css`, `sw.js` and `manifest.json` byte-unchanged and `index.html` is one sentence of Bulk Add help copy. Carried forward, deliberately NOT fixed: slash date + `exp:` remains `attention` (the one place `exp:` is not the top of the ladder — `8/8/26` cannot be read at all, whereas `May 5 12` reads fine and is merely implausible; carried forward by explicit operator instruction), `Trail Mix May 5 26` remains the accepted trailing-date tradeoff now narrowed to plausible years, historical pantry names are never re-parsed, input-path consolidation remains separate work, and the two-tap pantry-card collapse and unknown-quantity paired rows carry over from TASK-053. (2026-08-25)
- ✅ TASK-053 — Inventory Quantity Truth (D-069): two dogfooding complaints that turned out to be the same idea from opposite sides — quantity truth traded away for convenience. The brief said explicitly not to assume which layer caused "editing quantity does not reliably take effect", and that mattered: the pantry card's own editor was driven through typed-then-Tab, Enter, row-tap-to-collapse, tab switch, two consecutive edits, touch and keyboard, float / `buy_` / `ib_` / `staple_` ids, string / null / zero quantities, duplicate names, a twenty-item list scrolled to the middle, 390px and 1280px, **and against the deployed site** — `updatePantryQty()` persisted, re-rendered and survived reload every time. It was never the bug. The writer actually losing data was `confirmAddIngredientToPantry()` (Price Book "Add to pantry"), which deleted EVERY same-name record and pushed a rebuilt one to change a single field: on `Eggs 6 pcs · Expires Aug 28` that meant a new id, no `expiryDate`, no `dateMode`, no `staple`, no `stockLevel`, `purchaseDate` reset to today and `shelfLifeDays` recomputed from a blank category (20 → 3) — the printed expiry vanished and freshness restarted, and with two same-name rows both were destroyed. It now edits in place and stamps. The second complaint ("Eggs already in pantry — skipped" after buying twelve more, so the purchase was in inventory in no form at all) is fixed by reusing D-057's grocery boundary rather than inventing a second policy: `canMergePurchase()` judges the existing record unchanged, and `canMergePurchaseInto()` adds only the three facts the incoming purchase knows — its own printed expiry (one record cannot hold two dates), its unit, its explicit storage. `findMergeableStock()` picks the copy a purchase can honestly join instead of whichever sorts first; `applyPurchaseToStock()` folds it in IN PLACE and never rewrites `purchaseDate`, so the oldest portion still governs freshness. Bulk Add's duplicate branch is now three-way: top up when both quantities are known and the merge is safe (`merged`), `skipped` when the line carries no quantity (nothing to add, and folding "unknown" into a known 6 would replace a real number with null), and its OWN record when merging would lie — which is why `Eggs 6 pcs/Aug 28` plus `Eggs 12 pcs Sep 10 2026` stays two truthful rows and never becomes 18 under either date. A merge is reported as "1 stock item updated", never as a skip, and resolves the line so it leaves the D-068 retry textarea. Units are added, NEVER converted — `unitConvertFactor()` is the price path and `getUnitConversion()` returns 1 for anything unrecognised, so neither qualifies as tested for pantry quantities and 500 g + 1 kg stays two records rather than becoming 501; that same gate closed the identical latent lie already shipping in grocery check-off. Explicitly out of scope and not built: lot/FIFO architecture, a second conversion table, any new `AppState` collection. Reversible D-032 `done`, operator-directed landing. Local suite 283→306; mutation-checked twice (reverting the Price Book fix fails exactly the field-preservation test; restoring the duplicate-skip fails 12 merge tests). Merged `--no-ff` at `0fe2a63` with `bf4ad68` and `4b0d761` unchanged. **CI caught a real miss in the reviewed branch**: the LOCAL partial-retry spec was updated to the new duplicate policy but its PRODUCTION mirror was not, so the post-deploy gate failed with the deployed build correctly reporting "2 items added · 1 stock item updated · 1 already in pantry" against a smoke still expecting the old string — shipped behaviour right, assertions stale; fixed in `006f779` together with a new 10-case D-069 production smoke (Price Book edit-in-place, safe merge, explicit-expiry separation, unknown quantity, unit safety, 390px). Final main `006f779`; deployed `app.js`/`style.css`/`index.html`/`sw.js` verified SHA-256-identical; production smokes 123 passed / 4 skipped. No red-zone line touched — tombstones, `cloudReady`, `saveData()` semantics, the Firestore transaction/merge, auth and the service worker are all unchanged. Carried forward, deliberately NOT fixed: the two-tap pantry-card collapse (the `change` handler's `renderPantryKeepOpen()` replaces `#pantry-list.innerHTML` before the tap lands as a click — the value IS saved, but this is the most likely reason a successful edit LOOKS broken, which is how this task began), the unknown-quantity paired row, `updateBrowserItemQty()`'s 0-as-unknown and untrimmed name match, and the ~27px number input at 390px. (2026-08-25)
- ✅ TASK-052 — Bulk Add partial-retry (D-068): Bulk Add persisted the valid lines, held the modal open because a sibling line warned, and left the textarea COMPLETELY untouched — so pressing Add Items again resubmitted the lines that had already succeeded and reported "already in pantry — skipped", with nothing saying which items had landed and which still needed fixing. Characterising all eight cases through the real modal found what the brief's model could not accommodate: **two warning paths warned and then added the item anyway**, neither returning — `"Milk 2 L 8/8/2026"` was added as `name="Milk 2 L 8/8/2026"` with the shared expiry substituted, and `"Eggs, 12, pcs exp:2026-02-31"` was added as `Eggs` with the shared expiry standing in for the rejected date. Both are meant to be corrected and resubmitted, but their records already existed, so a correction produced a junk record plus a clean second copy (Milk: different names, duplicate guard never fires) or a confusing bounce (Eggs). A line cannot both be kept for correction and already be committed, so Bulk Add changes from **warn + add anyway** to **actionable warning = hold the line back for correction** — operator-reviewed and explicitly approved, not to be restored. D-067 parsing is untouched: the invalid date is still rejected and the ambiguous one still never guessed. The contract now: `added` → committed once and removed from the retry textarea; `skipped` (already in pantry) → resolved and removed, but still reported in the summary and its own note; `attention` → NOT committed, stays for correction in original text and original order. Classified by explicit status, never by matching warning wording. Valid lines are still persisted when a sibling needs work — tolerant, not transactional, which is the whole reason the textarea must be pruned. Feedback is one line from three counts ("2 items added · 1 already in pantry · 1 line needs attention."), toasted when the modal closes and inline above the notes when it stays open; per-line notes dropped their "Line N:" prefix because once the textarea is pruned those numbers point at text no longer on screen. Shared Storage/Expiry survive a partial submit and still apply on the retry; `openBulkAddModal()` remains the only reset. All-duplicate now closes instead of trapping the user in a retry that cannot progress. Zero new persisted state (`results` is a local array); `index.html`, `sw.js`, `manifest.json` byte-unchanged; no Firestore/`saveData()`/`cloudReady`/tombstone/auth/freshness/parser identifier touched. Two D-067 assertions that encoded the old warn-and-add behaviour were updated with the reason recorded inline, their parser halves unchanged. Reversible D-032 `done`. Local suite 265→283; 18 new cases, mutation-checked at 7-of-18 failing (the retry assertion had to be sharpened to the summary text, because the notes panel is cleared when the modal closes). Carried forward, deliberately NOT fixed: slash dates unsupported, no year sanity range, historical free-text records not migrated, skipped-line detail is transient once the modal closes, and a typo'd but otherwise valid name still adds normally (it is in inventory, and the Inventory card is the editor — Bulk Add is an input surface). (2026-08-25)
- ✅ TASK-051 — Bulk Add date truth (D-067): TASK-050 fixed the quick-add path but Bulk Add kept its older text parser, so the identical Kitchen Truth failure survived at the other entry point — `eggs 12 pcs aug 8 2026` stored the WHOLE string as `name` with quantity null and no expiry, then `inferCategory()` loose-matched "eggs" → Protein and `categoryShelfLife('Protein')` supplied 3 days from today, rendering "Best by Aug 28 · 3d left" for eggs dated Aug 8. Characterisation (driving the real `confirmBulkAdd()` in a browser) found two more defects the brief did not name: `eggs, 12, pcs, aug 8 2026` parsed name/qty/unit correctly and then DISCARDED the fourth comma field with no warning at all — silent, so nothing on the card looks wrong — and `exp:2026-02-31` passed the old `!isNaN(new Date(...))` guard and stored a date the D-066 renderer showed as "Expires Mar 3", because `new Date()` rolls over rather than erroring. Bulk Add also had NO parser test coverage of any kind; every pre-existing spec matching "bulk" tests bulk *cleanup*, an unrelated feature. Fixed with `parseTrailingDate()`, which recognises exactly three shapes in trailing position only (`aug 8 2026` / `8 aug 2026` / `2026-08-08`, plus ordinals and `Sept.`); on a match the date is stripped and the remaining text goes through the EXISTING quantity/unit parser — the comma path and `NO_COMMA_RE` are byte-unchanged — and on no match the text is left completely alone. Deliberately NOT an NLP layer: requiring a month WORD (or full ISO) plus a four-digit year is the entire safety argument, and it is what keeps `7 Up`, `Heinz 57 Sauce`, `Formula 1 Protein`, `Vitamin B12`, `12 Grain Bread`, `Omega 3 6 9`, `Vitamin 2000` and `Sauce 12 2026` intact — loosening it to accept a bare trailing number would break all of them at once. `8/8/2026` is refused rather than parsed (day-first in half the world, month-first in the other; a wrong guess moves an expiry by up to eleven months), recognised only so `looksLikeAmbiguousDate()` can warn and name the two unambiguous spellings. Precedence, strongest first: line `exp:` → recognised trailing date → shared field → bought-date + shelf life, with a trailing date stripped even when `exp:` also appears so a typed date can never survive inside the name. `isRealCalendarDate()` round-trips through `Date` and now guards BOTH bulk-add date paths. One expiry model only — records land in the existing `expiryDate`/`dateMode` fields and render through the unchanged D-066 path. Zero new persisted state; no Firestore/`saveData()`/`cloudReady`/tombstone/auth/freshness code touched; `style.css`, `sw.js`, `manifest.json` byte-unchanged. Existing free-text records are NOT migrated. Reversible D-032 `done`. Local suite 244→265; 21 new cases, mutation-checked at 14-of-21 failing against unmodified `main`. Carried forward, deliberately NOT fixed: warnings keep the modal open even after a successful add (and a corrected resubmit then hits "already in pantry — skipped"), slash dates stay unsupported, no year sanity range (`aug 8 1801` is accepted and honestly rendered as long expired), historical free-text names are not migrated, and a product name genuinely ending in a month + year typed with no quantity is read as name + expiry. (2026-08-25)
- ✅ TASK-050 — Inventory expiry truth (D-066): a dogfooding screenshot — an item added as "eggs 12pcs august 10 2026" rendering that whole string as the item name next to "3d left" — characterised before fixing, which changed what the defect was. The display ambiguity was the SMALLER half: `addToPantry()` read `#pantry-input` verbatim into `name`, so the string WAS the name — `quantity` stayed null, `unit` stayed '', `expiryDate` was never set; `inferCategory()` still loose-matched "eggs" inside the string and returned Protein, whose `categoryShelfLife()` default is 3 days, and THAT (not the typed date) produced "3d left". Replayed against the shipped code the same item entered with its real date is "Expired 14d ago" — so the badge was not ambiguous, it was WRONG, claiming three days of life for food whose printed date had passed. No schema change was needed: `name`/`quantity`/`unit`/`expiryDate`/`dateMode`/`purchaseDate`/`shelfLifeDays` all already existed and the bulk-add path already populated them; the quick-add form was collecting one of seven. Fixed in two parts. (1) Optional `#pantry-qty`/`#pantry-unit`/`#pantry-expiry` on the add row, stored in their own fields, producing the same record shape `confirmBulkAdd()` already produced so the two manual entry points stop disagreeing — blank still means unknown (no quantity → `null`, no date → bought-date mode), so D-057's "never invent a number" rule holds and storage is still inferred, never asked; new `pantryExpiryInfo()` branches on `dateMode` exactly as `pantryDaysLeft()` does rather than building a second freshness model, so the card's date and its days-left badge are two renderings of one number (swept ±400 days across both modes); cards render `.pi-name`/`.pi-qty`/`.pi-date` separately, a printed expiry reading "Expires <date>" and a derived one "Best by <date>" in a quieter style, because labelling an estimate an expiry claims a certainty the app does not have. (2) Mobile pass: `.pantry-add-row` was `display:flex` with no `flex-wrap` and `.ing-name-wrap` carries a global `min-width:0` with a `width:100%` input, so five buttons crushed `#pantry-input` to 26px at 320px and 390px — pre-existing on unmodified `main`, initially deferred, then pulled into scope because structured fields are worthless if the row cannot be typed into on the phone this app is dogfooded on; two properties (`flex-wrap: wrap` + a min-width floor scoped to this row, since the same wrapper is reused by the custom-item modal) took it 26→188 at 320px, 26→179 at 390px, 45→203 at 414px, with 768px and 1280px byte-identical. Zero new persisted state; no Firestore/`saveData()`/`cloudReady`/tombstone/auth code touched. Reversible D-032 `done`. Local suite 238→244; mutation-checked twice (7-of-8 data cases fail against unmodified `main`; both width cases fail against the pre-wrap CSS). Carried forward, deliberately NOT fixed: pre-existing free-text pantry names are not auto-parsed or migrated (guessing would re-introduce the exact defect closed here), the structured form is taller on narrow phones (four wrapped lines at 390px), no parser/NLP layer was added, and the quick-add box still does not accept the bulk parser's `Name, Qty, Unit` grammar. (2026-08-24)
- ✅ TASK-049 — Test-infrastructure trust (D-065): make the automated loop trustworthy without touching product behaviour (product source diff vs `a292206` is empty). Three parts. (1) The brief's root cause was WRONG and checking it mattered: `run-claude.ps1` never invoked `Check-DocsConsistency.ps1` by bare name — it always used a full path, byte-identical to the halting commit, working under both PowerShell 5.1 (what the scheduler uses) and pwsh 7; experimentally only a bare-name call can produce the observed message and none exists in the repo, so the halt is NOT reproducible and its trigger stays unknown. The real defect was one line lower: the block's own comment said "it never halts automation" and then called `Halt-Automation`, so a docs-drift report killed the whole overnight run. Phase 3b now resolves the path once, tests existence explicitly, and warns-and-continues — proven under the scheduler's own PS 5.1 invocation across four cases including a reproduction of the exact 2026-08-23 error, all reaching downstream work with a WARN in log + DIGEST. (2) `npm test` was answering two questions at once (21 specs load the checkout, 10 fetch the deployed site), so it answered neither: split into Playwright projects — `npm test` == `test:local` is the deterministic offline branch gate (230 tests, ~58s), `test:prod` is the post-deploy gate (81); CI runs local first then prod after the Pages sleep, so nothing stopped being verified, and `tests/suite-classification.spec.js` fails the local suite if any spec is filed in the wrong gate. (3) All 16 remaining fixed 2500ms waits classified before editing — all were post-goto/reload initialisation, none network or intentional UX timing — and replaced by one shared readiness condition; the diff across those 9 specs is exactly 16 wait replacements plus 9 requires, no assertion touched, local suite ~2.4min → ~59s. Reversible D-032 `done`. Open items recorded not fixed: historical bare-name trigger unknown; Check-DocsConsistency's 16 mostly-noise findings need a precision pass; production tests inherently cannot validate an unmerged branch; shorter mid-test waits (500/600ms) unaudited; the three D-064-hardened specs keep their own condition helper. (2026-08-24)
- ✅ TASK-048 — Cooking-method discovery (D-060..D-064): the wave where the tests were green and the product was broken. On production `main`, the Cook quick-filter row rendered `display:none` with ZERO chips — the filters had worked since D-055, but all 26 seeded recipes carried no equipment/effort/activeTime metadata, so every chip matched nothing and hid itself. Every discovery test passed because each injected its own tagged fixtures; the new specs assert against the shipped `sampleRecipes` instead. Fixed in four parts: (1) primary cooking-method chips never hide — an empty one is muted with an empty state naming the editor field that fills it; Rice cooker matches rice-cooker AND rice-cooker-steamer, Instant Pot matches instant-pot AND pressure-cooker (presentation grouping over existing `equipment[]`, no new field); Lowest effort aligned to the same `recipeEffortScore() <= 2` gate Home's "Easiest" uses and sorted by hands-on work, not clock. (2) 14 low-effort starter recipes (ids 27–40): 4 rice cooker, 4 oven, 3 Instant Pot, 3 no-cook, each written FOR its appliance and proven by a grep of its own instructions; the 26 originals byte-identical. (3) Opt-in delivery for existing installs — `ensureStarterRecipes()` stays first-run-only, a compact Cook prompt adds only genuinely absent recipes, presence is a permanent skip, and `AppState.deletions` is honoured so a deleted starter recipe is never resurrected (re-adding with a fresh `updatedAt` would have BEATEN its tombstone under LWW). (4) Seed object isolation — `[...sampleRecipes]` shared every recipe object with the module constant, and the exploit was reproduced end to end: the starter pack added a recipe pre-scaled to 8 servings and already favourited, carried over from an unrelated edit in the same session. Zero new persisted state. Reversible D-032 `done`. Recipes 26 → 40; live chips Rice cooker 4 / Oven 4 / Instant Pot 3 / No-cook 3. Production smoke 15/15. Known follow-ups recorded, not fixed: 180-day tombstone horizon, Firebase multi-device untested locally, `defaultStorageData`/`defaultCookingHacks` shallow-copy audit, Lowest effort deliberately broad at `<= 2` pending dogfooding. (2026-08-23)
- ✅ TASK-047 — "What should we eat?" (D-059): one deterministic Home card answering the question with up to 3 explainable picks — Eat this first / Easiest / Something different — composed from getReadyFoodSuggestions(), getCookableRecipes(), getExpirySuggestions() and the D-055 effort/variety helpers. No AI, no parallel recommendation system, no freshness rule recomputed. Availability is a TIER not a weight (the first cut recommended a shopping trip over dinner); effort reads hands-on time, not total. Categories omitted rather than guessed. Zero new persisted state and displaying a pick consumes nothing. Existing Ready-to-eat / What-should-I-cook cards and the recipe quick-filters untouched. Reversible D-032 `done`. Known follow-up recorded, not fixed: Home now has three suggestion surfaces — dogfood first, consolidate in a later UX wave. (2026-08-22)
- ✅ TASK-046 — Food attention notifications (D-058): foreground-only expiry alerts consuming the existing Kitchen Truth model, one grouped notification per pass, a device-local dedup ledger (`mealPrepFoodAlerts`, no new synced state), Settings-only permission opt-in, and the PWA app-icon badge. Push infrastructure deliberately NOT built — a static GitHub Pages app cannot deliver notifications while closed without a new backend, and that is stated in the product UI as well as the docs. Owner-authorised D-032 red-zone merge (`sw.js`). Real-device check on Android remains an owner/manual item. (2026-08-22)
- ✅ fix(import): tombstone-override fix — re-imported IDs now clear their tombstones in `importData()` before the union; prevents `applyTombstones()` from silently removing signed-in users' imported data on every reload (D-019) (2026-07-02)
- ✅ fix(import): `saveData()` returns Firestore Promise; `importData()` awaits it before showing success toast — closes the race window where a refresh could land on the pre-import cloud version (2026-07-02)
- ✅ BQ-007–BQ-012 sprint — button variants (ghost/danger/success), white-on-sage contrast (WCAG), 44px tap targets (modal-close, fav, pantry-remove, cooked-remove, day-action, scaler), CSS variable aliases (15 undefined tokens resolved), empty state for ingredient browser, focus outline on ingcat inputs (2026-06-30)
- ⏭ BQ-013–BQ-016 (P3) — deferred per build notes: color token consistency, badge consolidation, spacing scale, modal sizing. All say "defer past stabilize phase" in their build notes. Stays in BUILD_QUEUE for next human review. (2026-06-30)
- ✅ BQ-001–BQ-006 sprint — bulk add parser (no-comma format), pantry card stays open on edit, storage guide hides for unrecognized items, recently added at top, bulk add expiry date field, unit input datalist (2026-06-27)
- ✅ BQ-001 — Price Book subtitle reframed as personal price reference (honest expectations; no store-compare promise) (2026-06-27)
- ✅ BQ-002 — Dashboard renders on first open: added `renderDashboard()` to signed-out, Firebase-unavailable, and `loadUserData()` init paths (2026-06-27)
- ✅ BQ-003 — Recipe JSON import fixed: replaced `confirm()` (silently blocked in iOS PWA) with `showConfirmDialog()`; shows recipe count in dialog body (2026-06-27)
- ✅ BQ-004 — Duplicate pantry name: `addToPantry()` now asks "add another?" via `showConfirmDialog()` instead of silent skip (2026-06-27)
- ✅ P2 Task 3 — Dismiss suggested grocery items: ✕ button, `suggestDismissed` pantry flag, skip re-add, auto-clear on restock (2026-06-25)
- ✅ P2 Task 2 — "Suggested" amber badge on auto-suggested grocery items (`.grocery-suggested-badge`, tooltip = reason) (2026-06-25)
- ✅ P2 Task 1 — `suggested: true` + `suggestedReason: 'low stock'` flag on auto-suggested grocery items (`syncStapleToGrocery`, `checkAndReplenishLowStock`) (2026-06-25)
- ✅ Alpha P1 — one onboarding gate: Help modal no longer auto-opens on first run when Kitchen Setup Wizard fires (`pantryOnboardingDone` gate in `initApp()`) (2026-06-25)
- ✅ "Sample" badge on the 26 seeded recipes (`isSampleRecipe()`, Job #3 clarity) (2026-06-25)
- ✅ Product prompts PP1–PP7 added to PROMPTS.md (Engineering + Product sections); part of AI Dev OS v1.0 (2026-06-25)
- ✅ Alpha quick wins — pantry add-feedback toasts + Price Book heading naming fix (2026-06-25)
- ✅ Pantry search — filter the kitchen by name (`#pantry-search`, Job "did I already buy X?") (2026-06-25)
- ✅ Live recipe count on the Cook tab (`#recipe-count`, updates with search/filter) (2026-06-25)
- ✅ Firestore write guard — stop cloud-data wipe on deploy/reload (`AppState.cloudReady`) (2026-06-25)
- ✅ Pantry add row simplified; ingredient browser modal (2026-06-24)
- ✅ Weekly nutrition totals, grocery A→Z sort, recipe detail scaler, bulk pantry add, cook history (2026-06-23)
- ✅ Recipe favorites, buy-it button, global error handler, Mung Beans, text search, bug fixes (2026-06-22)
- ✅ Password reset, expiry suggestions, grocery→pantry transfer, paste-parser confidence, Filipino nutrition entries (2026-06-22)
- ✅ Phase C — pantry auto-deduction on cook (`markRecipeCooked()` → `deductIngredientsForRecipe()`) (2026-06-22)
- ✅ Ready Food Protein Identity **hardening** — correctable/pinnable batch protein (`setCookedProteinType()`), Auto-clears-the-field semantics, generated selector vocabulary, category normalization and strict type validation (merge `9021a90`, reviewed `c742f17`, D-072) (2026-08-27)
