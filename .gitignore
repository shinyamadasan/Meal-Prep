node_modules/
test-results/
playwright-report/
claude-session.log
automation.lock
.last-phase-result.txt
.scope-note.txt

# Local agent tool state (per-machine, not project content)
.claude/
.codex/

# External tool/repo that landed in this working directory, not part of this project
avoid-ai-writing/

# Auto-generated ops snapshot (regenerated by tools/Generate-Ops-State.ps1)
OPS_STATE.md

# Hand-testing payload for the recipe-import Worker (curl -d @recipe-request.json).
# Untracked since it was created on 2026-08-09; kept locally, ignored so it stops
# tripping the overnight automation clean-tree preflight. Root-anchored on purpose.
/recipe-request.json
